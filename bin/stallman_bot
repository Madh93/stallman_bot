#!/usr/bin/env ruby

require 'stallman_bot'
require 'stallman_bot/arg_parser'

args = StallmanBot::ArgParser.parse(ARGV)

StallmanBot::StallmanBot.new do |bot|
  bot.debug = args[:debug]
  bot.token = args[:token]
end.run
