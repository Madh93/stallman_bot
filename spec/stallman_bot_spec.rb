require "spec_helper"

describe StallmanBot do
  it "has a version number" do
    expect(StallmanBot::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
